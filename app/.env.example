# Leave VITE_API_URL unset in local dev — the Vite proxy handles /api routing.
# Set this in Vercel project settings for production deployment.
# VITE_API_URL=https://chuck-us-the-meeples.onrender.com
